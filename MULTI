// components/page-header.tsx
import React from 'react';
import { PALETTE } from './palette'; // Assuming PALETTE is defined somewhere

const PageHeader = ({ title }) => {
  return (
    <div style={{ backgroundColor: PALETTE.navy, color: 'white' }}>
      {title}
    </div>
  );
};

export default PageHeader;

// app/(app)/layout.tsx
import React from 'react';
import { PALETTE } from './palette'; // Assuming PALETTE is defined somewhere

const Layout = ({ children }) => {
  return (
    <div style={{ backgroundColor: PALETTE.navy }}>
      <div className="flex-1 overflow-y-auto">
        {children}
      </div>
    </div>
  );
};

export default Layout;

// app/(app)/page.tsx (Dashboard)
import React from 'react';
import { PALETTE } from './palette'; // Assuming PALETTE is defined somewhere
import Wrench from './icons/Wrench'; // Assuming Wrench is an imported icon component

const Dashboard = ({ nextMaintDate }) => {
  return (
    <main style={{ backgroundColor: PALETTE.navy }}>
      <div className="mt-4 rounded-lg border bg-white p-4 shadow-sm">
        <div className="flex items-start gap-3">
          <div className="mt-0.5 rounded-full bg-[rgba(44,62,135,0.1)] p-2">
            <Wrench className="h-5 w-5" style={{ color: PALETTE.purple }} />
          </div>
          <div className="flex-1">
            <p className="text-xs" style={{ color: PALETTE.textSecondary }}>Tu siguiente mantenimiento</p>
            <p className="text-sm font-medium" style={{ color: PALETTE.textPrimary }}>{nextMaintDate}</p>
            <button
              className="mt-3 inline-flex items-center justify-center rounded-full px-4 py-2 text-sm font-semibold shadow"
              style={{ backgroundColor: PALETTE.green, color: "white" }}
              onClick={() => alert("Cotización enviada (demo)")}
            >
              Cotiza ahora
            </button>
          </div>
        </div>
      </div>
    </main>
  );
};

export default Dashboard;

// app/(app)/mi-ipad/page.tsx
import React from 'react';
import { PALETTE } from './palette'; // Assuming PALETTE is defined somewhere

const MiiPad = () => {
  return (
    <main style={{ backgroundColor: PALETTE.navy }}>
      <div style={{ color: "#FFFFFF" }}>
        {/* Outermost container text */}
      </div>
      {/* Cards and other content */}
    </main>
  );
};

export default MiiPad;

// app/(app)/reparacion/page.tsx
import React from 'react';
import { PALETTE } from './palette'; // Assuming PALETTE is defined somewhere

const Reparacion = () => {
  return (
    <main style={{ backgroundColor: PALETTE.navy }}>
      {/* Header and other content */}
    </main>
  );
};

export default Reparacion;

// app/(app)/cashback/page.tsx
import React from 'react';
import { PALETTE } from './palette'; // Assuming PALETTE is defined somewhere

const Cashback = () => {
  return (
    <main style={{ backgroundColor: PALETTE.navy }}>
      <div style={{ color: "#FFFFFF" }}>
        {/* Section-level text outside cards */}
      </div>
      {/* Cards and other content */}
    </main>
  );
};

export default Cashback;

// app/(app)/calendario/page.tsx
import React from 'react';
import { PALETTE } from './palette'; // Assuming PALETTE is defined somewhere

const Calendario = () => {
  return (
    <main style={{ backgroundColor: PALETTE.navy }}>
      {/* White cards with content */}
    </main>
  );
};

export default Calendario;

// app/(app)/perfil/page.tsx
import React from 'react';
import { PALETTE } from './palette'; // Assuming PALETTE is defined somewhere

const Perfil = () => {
  return (
    <main style={{ backgroundColor: PALETTE.navy }}>
      {/* White cards with content */}
    </main>
  );
};

export default Perfil;
